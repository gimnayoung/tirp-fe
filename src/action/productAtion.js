import * as types from "../constants/product.constants";
import api from "../utils/api";

const createProduct = (formData)=>async(req,res) => {
    return async (dispatch) => {
        try {

        } catch (error) {
        }
    };
};

export const productAtion = {
    createProduct
};